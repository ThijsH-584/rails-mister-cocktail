<div class="container">
	<%= simple_form_for(@cocktail) do |f| %>
	  <%= f.input :name %>
	  <%= f.association :ingredient %>
	  <%= f.button :submit %>

	<%= link_to "Back to lists", cocktails_path %>

	<% end %>
</div>
