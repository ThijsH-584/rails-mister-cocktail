<div class="container">
	<%= simple_form_for([@cocktail, @dose]) do |f| %>
	  <%= f.input :description %>
	  <%= f.association :ingredient %>
	  <%= f.button :submit %>

	<%= link_to "Back to lists", cocktails_path %>

	<% end %>
</div>
